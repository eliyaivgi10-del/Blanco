# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**Blanco** is a TV show project about the creator's life. The repository is in its earliest stage — no application code exists yet.

The file `בלנקו  5⁩ 4.pdf` is a script or document related to the show (filename is in Hebrew).

## Language & Tooling

The `.gitignore` is the standard GitHub Python template, indicating this project will be built with **Python**. Common frameworks covered by the gitignore include Django, Flask, and Scrapy, along with tools such as pytest, ruff, mypy, and poetry/uv/pdm for packaging.

No commands for building, testing, or running the project are defined yet — update this file once a package manager, framework, and test runner are chosen.
